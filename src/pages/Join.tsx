import JoinForm from "@/features/users/JoinForm";
import FullPageForm from "@/ui/FullPageForm";

const Join = () => {
    return (
        <FullPageForm>
            <JoinForm />
        </FullPageForm>
    );
};

export default Join;
