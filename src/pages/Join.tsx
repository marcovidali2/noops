import JoinForm from "@/features/auth/JoinForm";
import FullPageForm from "@/ui/FullPageForm";

const Join = () => {
    return (
        <FullPageForm>
            <JoinForm />
        </FullPageForm>
    );
};

export default Join;
