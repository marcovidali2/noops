import JoinForm from "@/features/users/JoinForm";
import FullPageForm from "@/ui/ResponsiveForm";

const Join = () => {
    return (
        <FullPageForm>
            <JoinForm />
        </FullPageForm>
    );
};

export default Join;
